{
  "id": "c61b4306",
  "title": "Introduce entity descriptor objects",
  "tags": [
    "architecture",
    "deep-modules"
  ],
  "status": "done",
  "created_at": "2026-05-01T19:45:51.245Z"
}

## Done

Created entity descriptor objects that encapsulate all type-specific behaviour behind a single object per entity type.

### New files
- `src/entities/descriptor.ts` — `EntityDescriptor` interface + `RawFrontmatter`, `RelFieldDef`, `RelationDisplay` types
- `src/entities/requirement.ts` — requirement descriptor
- `src/entities/assumption.ts` — assumption descriptor
- `src/entities/decision.ts` — decision descriptor
- `src/entities/idea.ts` — idea descriptor
- `src/entities/stakeholder.ts` — stakeholder descriptor
- `src/entities/risk.ts` — risk descriptor
- `src/entities/term.ts` — term descriptor
- `src/entities/registry.ts` — descriptor registry + utility functions (`getDescriptor`, `cleanRefs`, `renameRefs`, `hasRelation`, `VALID_EDGES`, `getRelField`, `getAllRelFields`, `ENTITY_CONFIG`)

### Refactored files (switch statements eliminated)
- `src/io/parser.ts` — uses `desc.parse()` and `desc.serialize()` instead of 2 switches
- `src/io/files.ts` — uses `ENTITY_TYPE_ORDER` and `allDescriptors()` instead of hardcoded lists
- `src/graph/graph.ts` — uses `desc.edges()` instead of per-type switch
- `src/display/format.ts` — uses `desc.ansiColor` and `desc.detailRelations()` instead of 3 switches
- `src/commands/remove.ts` — uses `cleanRefs()` from registry
- `src/commands/rename.ts` — uses `renameRefs()` from registry
- `src/commands/link.ts` — uses `getRelField()`, `getAllRelFields()` from registry
- `src/commands/list.ts` — uses `ENTITY_TYPE_ORDER` and `allTypes()`
- `src/commands/edit.ts` — uses `allDescriptors()` for folder search
- `src/commands/init.ts` — uses `allDescriptors()` for folder creation
- `src/commands/status.ts` — uses `ENTITY_TYPE_ORDER` and `allTypes()`
- `src/commands/add.ts` — uses `desc.parse()` for entity construction
- `src/search/fuzzy.ts` — uses `hasRelation()` from registry
- `src/mcp/server.ts` — uses `desc.jsonFields()` and `desc.parse()`
- `src/types.ts` — `ENTITY_CONFIG` now re-exported from registry

### Result
- Adding entity type #8 = create one descriptor file, register it, done
- 91 tests pass, CLI verified working
- `switch` on entity type reduced from 16 locations to 2 (add.ts prompt logic, import.ts ADR format)
