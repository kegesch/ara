{
  "id": "e2cb271b",
  "title": "Validate A-012: test ADR import against real-world ADR files",
  "tags": [
    "validation",
    "import"
  ],
  "status": "done",
  "created_at": "2026-05-01T18:33:49.679Z"
}

## Done

- Created `tests/import.test.ts` with 29 test cases covering:
  - Standard Nygard format (numbered titles, accepted/superseded/proposed/deprecated/rejected)
  - Superseded variants: curly brace `{superseded by N}`, markdown links, ALL CAPS, common typo "superceded"
  - Missing ## Status section → defaults to accepted
  - Title extraction from `#` heading, filename fallback (strips leading numbers, replaces dashes/underscores)
  - Extended ADR format with extra metadata sections
  - Minimal ADR (just heading + text)
  - Edge cases: empty content, whitespace-only, inline status format, multi-line status
  - Case-insensitive ## Status heading
  - adr/tools template format
- Fixed parser to handle the common "superceded" typo (missing second 's')
- Exported `parseAdr` and `ImportedEntity` from import module for testability
- Validated A-012 via `arad validate A-012`
- All 140 tests pass (29 new + 111 existing)
